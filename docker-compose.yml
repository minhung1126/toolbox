services:
  toolbox:
    image: ghcr.io/minhung1126/toolbox:latest
    container_name: toolbox
    ports:
      - 8000:8000
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    restart: unless-stopped
